spring:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    url: ${URL}
    username: ${USERNAME}
    password: ${PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    port: ${REDIS_PORT}
    host: ${REDIS_HOST}
  mail:
    host: ${MAIL_HOST}
    port: ${MAIL_PORT}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
server:
  port: 8080
token:
  secret: ${SECRET}
  exp:
    access: ${ACCESS_EXPIRED} # 1800ms * 1000 == 30min
    refresh : ${REFRES_EXPIRED} # 604800ms * 1000 = 1 week